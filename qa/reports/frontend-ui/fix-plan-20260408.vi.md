# Ke hoach fix frontend UI sau rerun 2026-04-08

## Nguyen tac

- Fix theo thu tu: `helper/harness` -> `spec drift` -> `seed stability` -> `nghi van san pham`.
- Khong mo lai `test.md` backend neu chi thay drift o frontend e2e.
- Chi nang cap mot loi thanh bug san pham khi da loai helper/spec/seed drift.

## P0 - Fix de on dinh bo test

1. `support/api.ts`
- Cho phep `apiJson(request, path, { method, body, expectedStatus })` hoat dong voi public endpoint khong co `session`.

2. `playwright.config.ts`
- Uu tien do tin cay hon toc do.
- Dat `workers: 1` mac dinh cho suite frontend UI, hoac toi thieu co co che ep `1 worker` cho nhom spec request-heavy/flood.
- Neu can, tach `outputDir` rieng va tranh artifact cleanup race.

3. `support/scenario-helpers.ts`
- Tang do ben seed session:
  - nhieu ngay hon
  - random offset slot ban dau
  - fallback doi teacher neu va cham lien tuc

## P1 - Fix spec drift ro rang

1. `landing-messages-products-ui.spec.ts`
- Doi payload landing submit sang `parentName`, `parentPhone`, `tracking`.
- Doi query teaching reports sang co `startDate/endDate`.
- Loai bo `parent2`; tao parent thu hai bang fixture that.
- Doi ky vong role cho teaching reports theo controller hien tai.
- Xem lai `SALE GET /products` timeout sau khi giam ENOENT.

2. `bulk-agents-class-config-ui.spec.ts`
- Sau khi fix helper public `apiJson`, rerun `15.4`.
- Doi payload supplier quote sang contract moi (`title`, `quoteDate`, `itemName`, `unit`).

3. `expenses-rbac-ui.spec.ts`
- Sua case `GET /expenses/stats` theo helper moi.
- Thu hep selector audit log chi vao action button trong row, khong bat `Xoa loc`.
- Sua request `POST /ads/accounts`:
  - dung `session.cookieHeader`
  - gui body voi `platformAccountId`
- Doi `networkidle` o cac case guard timeout bang assert redirect/body ro hon.

4. `state-machine-webhooks-ui.spec.ts`
- Thay `toBeTrue()` bang matcher Playwright hop le.
- Doi route cu sang route hien tai neu con sot.
- Sua expectations theo contract hien tai:
  - invoices cancel
  - duplicate leads
  - chatbot settings
  - attendance expired message shape
  - wallets list assertion

## P2 - Tai hien va xac minh nghi van san pham

1. `release-gate-ui.spec.ts`
- Them buoc reload/refresh attendance page sau submit public token.
- Neu reload xong van khong len `Co mat: 1`, nang cap thanh UI sync bug.

2. `scenarios-10-3-to-12-4.spec.ts`
- Giữ focused repro cho `Concurrent Order Approve`.
- Do 3 thu:
  - so invoice tao ra
  - so lan tang/giam wallet
  - so enrollment side effect
- Neu side effect van idempotent, ha assertion.
- Neu side effect duplicate, mo bug san pham.

3. `expenses-rbac-ui.spec.ts`
- Rerun rieng 2 case timeout sau khi sua wait strategy.
- Neu van timeout o 1 worker, mo bug UI/route guard/performance.

## Thu tu thuc thi

1. Fix `support/api.ts`.
2. Chot cau hinh worker/artifact cho Playwright.
3. Sua `landing-messages-products-ui.spec.ts`.
4. Sua `bulk-agents-class-config-ui.spec.ts`.
5. Sua `expenses-rbac-ui.spec.ts`.
6. Sua `state-machine-webhooks-ui.spec.ts`.
7. Rerun tung spec da sua.
8. Cuoi cung moi quay lai 3 nghi van san pham.

## Trang thai thuc thi

Da hoan thanh:
1. fix helper chung
2. fix spec drift
3. on dinh wait strategy / throttle retry
4. rerun lai batch triage

Ket qua:
- `162/162 PASS` cho batch triage:
  - `e2e/release-gate-ui.spec.ts`
  - `e2e/landing-messages-products-ui.spec.ts`
  - `e2e/expenses-rbac-ui.spec.ts`
  - `e2e/bulk-agents-class-config-ui.spec.ts`
  - `e2e/crisis-opex-flow-ui.spec.ts`
  - `e2e/materials-export-students-ui.spec.ts`
  - `e2e/specs/b06-b11-followup-ui.spec.ts`

Buoc tiep theo neu muon mo rong coverage:
- sync checklist/evidence
- chay sang lane khac con mo trong `testfrontendui.md`
