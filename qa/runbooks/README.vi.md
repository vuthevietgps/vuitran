# Runbooks

Thư mục này gom các kịch bản chạy thực tế theo từng mục đích:

- `frontend-ui/`: batch runbook để thực thi checklist UI và quay evidence.
- `demo-flows/`: kịch bản quay demo hoặc showcase theo storyline.

Checklist vẫn là nguồn `what to test`, còn runbook là `how to run theo luồng`.
